# Incident resolver

One address per incident. The identifier is a letter (after `Z` — `AA`, `AB`, …), the anchor `inc-<lowercase letter>`. A rule references an incident only by the anchor of this table: from `docs/*.md` — `incidents.md#<anchor>`; from `README.md`/`CHANGELOG.md` — `docs/incidents.md#<anchor>`; from a skill — as text `docs/incidents.md#<anchor>` (the skill is read outside the repository). The mapping of a letter to the tracker card, date, and primary record is kept in the private workspace registry, outside this repository; a rule references only the letter. The "Symptom" and "Consequence" columns are the basis of a rule: the rule is derived from them without reading the private table. The "Rules" column is reference-only (held by review, not CI).

Past rows are not rewritten retroactively: an error is a new row referencing the old one. A new incident is the next letter here and a row in the private registry (`README.md`, "How to reference an incident").

| Anchor | Id | Symptom | Consequence | Rules |
|---|---|---|---|---|
| <a id="inc-a"></a> | A | the review-round score journal was kept in a tracker card | the address died when the workspace moved; the journal moved to the scorecard repository — one row per run, one PR | `review-cycle.md#scorecard`; mentions in `pipeline.md`, layer 1 |
| <a id="inc-b"></a> | B | the parent was created not in `backlog` and assigned to the PM; a sub-issue was completed in `in_review` | two PM runs created two identical first stages, both worked for nothing; the stage barrier did not close, the PM was not woken | `pipeline.md#start-order`, `#children-first`, `#barrier-terminal`; layer 1 `in-review-not-terminal`, `parent-backlog-first`, `done-line-in-body` |
| <a id="inc-c"></a> | C | three auditors on different variants of one model were asked about their model | two identical self-reports and one accidental match with the configuration default; the model is determinable only from `multica runtime usage` | `review-cycle.md#model-from-usage`; layer 1 `model-not-in-task` |
| <a id="inc-d"></a> | D | a skill was updated with `multica skill update` from a stale copy | another edit made the day before was wiped (two blocks, two section merges), noticed a day later by a manual reconciliation | layer 1 `skill-no-versions` |
| <a id="inc-e"></a> | E | the auditor panel worked, the Reviewer run died separately (`idle_watchdog`, then `529 Overloaded` three times) | the stage closed on statuses without a score line, registry, or personal reproduction; reproduced twice (specification round 3 and code round 1) | `pipeline.md#synthesis-stage`; layer 1 `barrier-by-status` |
| <a id="inc-f"></a> | F | after `blocked` the card was moved to `todo` directly and via `backlog → todo` | no task was created, the stage hung dead while the runtime was alive; the cure is `multica issue rerun` | layer 1 `rerun-after-promote` |
| <a id="inc-g"></a> | G | the owner decision was received, the specification written without mentioning the executor, the status moved | no run started, the pipeline idled 27 minutes until manual intervention | `pipeline.md#resume-by-mention`; layer 1 `mention-is-run`; `CHANGELOG.md` 2026-09-08 |
| <a id="inc-h"></a> | H | the code stage was being closed without green CI on the head of the last commit; the diff text (docblocks, PR title) was not part of the audit subject | owner decision 2026-09-14: the barrier is green CI on the PR head; the audit subject is the whole diff, including text | `pipeline.md#code-barrier`, `review-cycle.md#audit-subject`; `CHANGELOG.md` 2026-09-14 |
| <a id="inc-i"></a> | I | a card touching neither customers, nor money, nor availability received six stages and three specification rounds; the project tracker had a duplicate of the card | the owner decision of 2026-09-12 on the impact class and the round limit | `impact-class.md#classify-first`, `#hygiene`, `#limits-table`; `ownership.md#class-doubt`; `CHANGELOG.md` 2026-09-12 |
| <a id="inc-j"></a> | J | the audit stage was promoted with an empty body and no round envelope | an auditor assembled two security runs, including the escalation pool, on round 1 of the specification audit | `pipeline.md#audit-envelope` |
| <a id="inc-k"></a> | K | the card is `cancelled`, the executor unassigned | the run kept working and spending money; stopping exists only in the UI, there is no CLI command | layer 1 `cancel-does-not-stop` |
| <a id="inc-l"></a> | L | the owner moved the parent `in_review → done` | the next card did not start (`done` emits no events), the pipeline idled until a manual question; the second card of the same idle | `pipeline.md#next-card-on-in-review`; layer 1 `done-is-silent` |
| <a id="inc-m"></a> | M | `provider_quota_limit` in the middle of the day, the stage failed before starting | the backup executor lived only in the PM's head | `pipeline.md#backup-executor` |
| <a id="inc-n"></a> | N | the regulation lived in three copies (the PM instructions, the source-project file, the workarounds skill) | the copies contradicted each other; extraction into a repository, three layers; decisions D-7, D-8 | `impact-class.md#money-arch-auditor`; `CHANGELOG.md` 2026-09-18 (the extraction and reconciliation entries) |
| <a id="inc-o"></a> | O | tests were not verified by mutation | never-failing tests counted by name | `pipeline.md#mutation-by-run` |
| <a id="inc-p"></a> | P | 1037 silent `skipped` in a report | passed off as "all green" and brought the PR down | `pipeline.md#builder-report` |
| <a id="inc-q"></a> | Q | three reports in a row without the command named | hid a red coverage threshold | `pipeline.md#builder-report` |
| <a id="inc-r"></a> | R | `git switch` in a shared tree | dragged away someone else's HEAD; almost carried away someone else's migration | `pipeline.md#one-writer` |
| <a id="inc-s"></a> | S | mutations predicted, not run | predicted mutations did not go red; a prediction error became someone else's finding | `pipeline.md#mutation-by-run` |
| <a id="inc-t"></a> | T | two variants of one model in one round | overlapping findings at double the price | `review-cycle.md#one-per-family` |
| <a id="inc-u"></a> | U | the PR ↔ card link was written in the PR body | the body is not preserved at squash; the link is the slug in the title | `pipeline.md#pr-slug` |
| <a id="inc-v"></a> | V | a card number was invented before the card existed | the auto-numbering gave the number to someone else's work, twice in one day | `pipeline.md#pr-slug` |
| <a id="inc-w"></a> | W | the owner report and the card comment were separate actions | the task hung in Review for a day after the merge | `pipeline.md#comment-with-report` |
| <a id="inc-x"></a> | X | document edits in a shared tree after the commit | were lost | `pipeline.md#spec-in-commit` |
| <a id="inc-y"></a> | Y | an executor closed a blocked report with a plain-text question; no wakeup on the stage card | the PM was not woken by the block or by the report and idled until the owner nudged; the stage stood | `pipeline.md#blocked-mentions-pm`, `#stage-wakeup`; layer 1 `mention-is-run` |
